package com.bappli.saf.datalink.mappers;

public class ColumnsMapper
{

	/*
	//------------------------------------------------------------------------------------------- map
	public static Set<String> map(Class<? extends Object> objectClass, String[] columns)
	{
		Set<String> map = new HashSet<String>(); 
		Collection<Field> fields = ClassFields.accessFields(objectClass);
		for (String column : columns) {
			if (!column.contains(".")) {
				String getter = Getter.getGetter(column);
			} else {
				
			}
		}
		ClassFields.accessFieldsDone(objectClass);
		return map;
	}
	*/

}
